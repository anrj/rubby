{
  "name": "rubby-tests",
  "version": "1.0.0",
  "description": "Golden set evaluation and testing for Rubby AI",
  "type": "module",
  "scripts": {
    "eval": "bun run evaluation.ts",
    "eval:baseline": "bun run evaluation.ts --output metrics_baseline.json",
    "eval:watch": "bun run evaluation.ts && echo '\n\n🔄 Running again in 5s...' && sleep 5 && bun run eval:watch",
    "compare": "bun run compare-metrics.ts",
    "regression": "bun run regression-test.ts",
    "regression:strict": "bun run regression-test.ts --strict",
    "test": "bun run regression-test.ts"
  },
  "keywords": [
    "testing",
    "evaluation",
    "metrics",
    "golden-set"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
